# common-error-handling
Library that manage error handling for spring microservices


## How to use

### Locally 

Following command will create the library inside our local maven repository

`./gradlew publishToMavenLocal`

if you're using a Linux distribution, you can create an alias.

`alias publish-local="./gradlew clean build publishToMavenLocal"`
